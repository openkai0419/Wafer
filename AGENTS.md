# Instructions for contributors
- When modifying any Python file under `source`, update the matching test in `test`.
- The test name uses `test_<path_with_underscores>.py`.
- Keep comments minimal and in English only when required.
- Adjust tests to reflect code changes.
