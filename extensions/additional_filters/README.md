## Additional Filters

extra query filters for searching.

### Filters
- **Date Range** — Filter by date fields using calendar UI, presets (today, last week, etc.) or custom ranges
- **Regex** — Pattern-based field matching with optimized literal extraction for fast queries
