# Color Search

Extracts up to five representative colors per image and stores them as content-hash tags.

Each palette slot is stored as a packed RGB integer under `color.palette.1` through `color.palette.5`. The filter UI displays colors as hex swatches and filters by RGB distance tolerance.
