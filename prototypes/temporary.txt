prototypes 仕様メモ (source ではなく試作コードの設計と意図)

対象ファイル
- prototypes/command/core.py
- prototypes/command/menu.py
- prototypes/command/ui.py
- prototypes/command/state.py
- prototypes/utils.py
- prototypes/mouseeventmanager.py
- prototypes/binding/editors.py

command/core.py の要点
- CommandParam / CommandMeta / CommandBase
- CommandRegistry: 登録と実行のみ（Undo 削除）
- create_command_from_callable / register_command_defs

command/menu.py の要点
- _split_parts / _is_sep_token / _sep_path / _is_section_token / _section_parts
- _chain_providers
- RegistryBackedMenu: 定義正規化と登録、path_prefix 適用
- MenuHub: パス集約・フォルダ展開

command/ui.py の要点
- CommandOptionsDialog: パラメータフォーム生成
- ボタン: 「実行」「保存」「キャンセル」
  - 実行: 保存せず実行のみ。ダイアログは閉じない。execute_callback経由で実行
  - 保存: 実行せず保存のみ。ダイアログを閉じる（Accepted）
  - キャンセル: 何もしないで閉じる（Rejected）
- CommandMenuBuilder: 行ウィジェット/セクション/区切り/チェック/オプション
- CommandMenuRow: 表示専用
- MenuBuilder: 高レベル一括生成/ポップアップ

オプション編集の永続化
- CommandOptionStore(prototypes/command/state.py): 統一形式 {id,args} で保存。get() は args のみ返し、get_payload() で {id,args} を取得
- CommandOptionsDialog: 初期表示に前回保存 payload.args を反映
- CommandMenuBuilder:
  - 通常実行時は保存済み args を読み込みコンテキストで上書き実行
  - ダイアログ「実行」は保存せず実行のみ
  - ダイアログ「保存」は実行せず保存のみ

追加: MenuBuilder（menu/ui.py）
- 空のQMenuを保持し、一括生成のみを提供（demo_appで使用している最小API）
- API
  - MenuBuilder(parent=None, context_provider=None)
  - menu: 生成先QMenuの参照
  - build([token|folder|path,...]) -> QMenu
    - token: "-" または "path/-"、"path/:Label"
    - command: "cmdId" または "folder/.../cmdId"
    - folder: "folder" または "a/b" を指定すると配下を展開
    - rebase: "temp/path" のように、末尾セグメントがフォルダならそのサブツリーを temp/path にリベース配置
    - 実コマンドの重複のみ例外。セクション/セパレータ重複は許容
    - 単一フォルダ名の呼び出しが複数ツリーに一致する場合は曖昧さエラー
  - build_all_roots() -> QMenu: すべての最親フォルダを自動列挙してメニュー化
    - items から先頭セグメントを抽出して順序保持
    - ルートに直下コマンドしかないものは対象外

  選択専用モード
  - CommandMenuBuilder/MenuBuilder は selection_callback を受け取り、選択時にコマンド実行せず callback(cmd_id) のみ呼ぶ
  - has_options や checkable は選択モードでは無効化（トグルや実行は行わない）
  - MouseBindingEditor は build_all_roots(selection_callback=...) を使い、ツリーからコマンドIDを選ぶ
  引数ラッパ
  - create_command_from_callable は func をラップし execute(self, **kwargs) で呼び出す（undo 引数は廃止）
  - meta が存在する場合 call_execute は meta.params に含まれる名前のみ抽出（params 空でもフィルタ実施）
  - params 未定義のコマンドへ追加コンテキスト(kwargs)を渡しても func が受け取らないなら無視され TypeError 回避
  ポップアップ共通化
  - MenuBuilder.popup_all_roots(anchor, selection_callback, context_provider=None) を追加
  - build_all_roots + メニュー位置計算(mapToGlobal) を統一的/再利用的に提供
  - 呼出側は anchor(例: ボタン) と callback だけ指定すればよい
  不具合修正
  - CommandMenuBuilder.build_into から参照される _get_or_create_submenu_chain が Row 側に存在していたため AttributeError が発生していた問題を解消。
  - 関連ヘルパーを Builder へ移設し、Row から削除。
  リファクタ（共通化）
  - _add_names: 重複検出と seen 追跡を一元化。リベース有無でエラーメッセージを切替。
  - _collect_folder: フォルダ展開と存在検証を一元化。
  - _flatten_for_use: use() 用のトークン再マッピングを関数化。
  - _build_into: chain_providers と build_into 呼び出しの共通化。
  - build()/use() の分岐を縮約し、挙動は等価のまま簡潔化。
  - 重複検出の不具合（raise 後に seen.add が到達不能）を修正。
  - CommandMenuBuilder._display_override: display_map 展開とサブメニュー生成処理を関数化。
  - 分割ロジックを split_parts に統一し文字列 split("/") の重複を排除。

エラーハンドリング（人為的ミスの早期検出）
- CommandRegistry.register: id 未設定や重複 id を検出すると例外
- RegistryBackedMenu.ensure_registered: 同一クラス内で重複する id を検出すると例外
- MenuHub.register_paths: 既に他クラスで登録済みの id を検出すると例外
- CommandMenuBuilder.build_into: 未登録のコマンド id が含まれると例外
- MenuBuilder.build: 不明なコマンド/フォルダ、リベース後の実コマンド重複は例外。単一名で複数一致は曖昧さエラー
- MenuBuilder.build_all_roots: 最親フォルダが一つも無い場合は例外

RegistryBackedMenu 定義の要点
- create_definitions は以下のいずれかを返す:
  - [str|def,...]
  - (defs, items)
- 必須: 各 def に path を明示。id は path 末尾から自動導出
  - 例: {path: "folder/sub/echo", meta, func} → id="echo" に正規化
- menu_path は廃止。path_prefix をクラスに付与可能（items/defs の相対指定を許容）
- フォルダ展開時はセクション(path/:Label)と区切り(path/-)を含めて忠実に再現

例
- class.path_prefix = "commands"
- defs: {path: "Options/echo", meta: CommandMeta(display="Echo"), func: ...}
- items: [":Options", "-", "Options/echo", "-"]

mouseeventmanager.py の要点
- ClickType: SINGLE/DOUBLE/WHEEL_UP/WHEEL_DOWN/DRAG_START/DROP
- MouseButton: LEFT/RIGHT/MIDDLE/X1/X2/NONE
- MouseActionKey: (button, click_type, held_buttons) で一意なキー。repr は "X1+RIGHT SINGLE" 形式。
- MouseEventManager: bind/unbind/clear と各種イベントの受け取り→キー生成→アクション実行。
- MouseEventDispatcher: 任意の QWidget を監視し、ドラッグ開始しきい値検出、ダブルクリック判定、ホイール/ドロップ転送を行う。

意図
- メニュー/コマンドは定義の単一化(create_definitions)と呼出時再構成の両立で拡張容易に。
- マウス入力はキー合成によりウィジェットごとのバインド差し替えを単純化。

editors.py の要点 (更新: JSON直接表示/保存)
- WidgetRef: ウィジェット参照ラッパ。
- MouseBindingEditor: コマンド選択後 has_options=True ならオプション編集し保存で payload {id,args} を保持。表示は常に JSON (dict payload は json.dumps、文字列idはそのまま)。内部は _payloads に辞書、表示テキストは JSON 化された文字列。
- ShortcutBindingEditor: セル UserRole に payload を保持し表示は JSON。Edit Options で再編集後 JSON 再生成。

store.py 拡張
- 値は文字列 id か payload 辞書。resolve はそのまま返す。

バインド時引数保持仕様 (更新: JSON統一)
- 全てのバインド値は JSON 文字列で保持・表示する。
- 共有モード: {"id":"cmd","args":{}} のように args が空の JSON。実行時は CommandOptionStore.get(id) で最新 args を取得して実行。
- 専用モード: {"id":"cmd","args":{...}} のように args が非空。実行時はそのままの引数で実行する。
- MouseBindingEditor は選択時、IDのみの場合でも {id,args:{}} に正規化して保持・表示する。
- ShortcutBindingEditor は従来通り JSON を保持（基本は専用モードに該当）。

ユーティリティ
- utils.py
  - is_json_text(s: str) -> bool
  - normalize_payload_dict(data: Any) -> {id,args}
  - to_payload_json(data: Any) -> str
  - read_json_file(path, default=None) -> Any  存在しない/失敗時は default
  - write_json_file(path, data, indent=2, ensure_ascii=False) -> bool  成功可否
  - show_error(parent, text, title="Error") -> QMessageBox.critical の薄いラッパ

CommandRegistry 拡張
- execute_payload(data, extra_context=None):
  - data が JSON 文字列の場合のみ文字列として受け付ける（非JSON文字列IDはエラー）。
  - 辞書の場合は {id,args} を直接使用。
  - id と args を抽出し、extra_context を後勝ちでマージして実行。
  - 従来 execute(name, **kwargs) は内部委譲用として残置。

CommandOptionStore 仕様
- get(id) は JSON 文字列 '{"id":"...","args":{...}}' を返す。
- get_payload(id) は辞書 {id,args} を返す。
- 永続化ファイル .command_options.json は indent=2 で整形保存。

mouseeventmanager.py 変更点
- MouseEventManager に set_resolver(resolver) を追加。直接 bind されていないキーを受け取った際 _resolver(key, event) をフォールバック呼び出し。

demo_app.py 変更点
- DemoPane に MouseBindingStore 参照と _resolve_fallback を追加し、未バインドキーでも共通または個別スコープ設定済みコマンドを実行可能に。
- MouseBindingEditor 呼び出しシグネチャ変更 (commands リストは内部選択メニューから取得するため不要)。
 - 初回起動で保存ファイルが存在しない場合、default_mouse_bindings() を MouseBindingStore の "*" スコープにシードし、以降の適用と編集は常に Store 由来の値を用いる。
 - MainWindow の menuBar は直接コマンドID "bindings", "shortcutBindings" を指定し一段構成化。menu_demo.MenuMenu のパスは "bindings", "shortcutBindings" に変更し重複フォルダ "binding" を排除。

CommandBindingMixin (binding/mixins.py) 追加
- 任意 QWidget でコマンド/マウス/ショートカットバインディング機能を共有化
- init_command_binding(scope_name=None) を呼ぶだけで利用可能。scope_name 未指定時は objectName かクラス名
- 提供API:
  - set_binding_scope(name) / binding_scope()
  - set_mouse_bindings(bindings: Dict[MouseActionKey, Any]) / get_mouse_bindings()
  - set_shortcut_bindings(dict) / get_shortcut_bindings()
  - exec_command(cmd) 明示実行（内部で _exec）
- DemoPane は QFrame, CommandBindingMixin の多重継承に変更し固有ロジック削減
- 元 DemoPane 内の _exec, フォールバック解決, バインド正規化処理を Mixin へ移動
- これにより他の QWidget 実装 (例: QSplitter, QScrollArea 内部ペイン) への再利用が容易

BindingManager (binding/manager.py) 追加
- mouse_bindings.json のロード/初期シード/適用/保存を集約
- load_or_seed(defaults) : ファイルが無ければ defaults を "*" スコープでシードして Store へ投入
- apply_mouse_bindings(widgets) : 各ウィジェットの scope 名 (binding_scope() 優先) に応じて最適なコマンドを適用
- clear_shortcuts(widgets) : 全ショートカット解除
- save() : 現在の内容をファイルへ書き出し
- デフォルトパス: prototypes/mouse_bindings.json
- DemoPane 初期化処理の共通化で MainWindow._setup_defaults は 4 行に縮約
 - シングルトン化と自動登録: BindingManager.instance() を導入し、WeakSet で登録ウィジェットを保持。CommandBindingMixin.init_command_binding() で自動 register。apply_registered()/clear_shortcuts_registered() で一括適用。

注意
- この仕様は prototypes のみ。source/ の正式実装には影響しない。
- 試作のため API は変更される可能性がある。
 
mouse_bindings.json 永続化 (更新)
- 保存タイミング: MouseBindingEditor で OK 押下時に現在の全バインドをファイルへ書き出す。
- 保存場所: prototypes ディレクトリ直下 (prototypes/mouse_bindings.json)。source 移行時に変更予定。
- フォーマット: 配列形式。
  [
    {"button":"RIGHT","click":"SINGLE","held":[],"scopes":{"*":"{...payload json...}","Widget A":"{...}","Widget B":"{...}"}},
    ...
  ]
- held は修飾ボタン列 (単独時は空配列)。
- scopes は scope->payload(JSON文字列) の辞書。"*" がグローバル、ウィジェット名が個別。
- 読込: 起動 (_setup_defaults) 時に存在すればロードして各 DemoPane に適用。存在しない場合は default_mouse_bindings() を使用。
- payload が空 args の JSON なら共有モードとして実行時に最新オプションへ差し替え、args 非空は専用モードとして固定。
